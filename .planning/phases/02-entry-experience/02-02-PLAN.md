---
phase: 02-entry-experience
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - public/js/app.js
  - public/css/theme.css
autonomous: false
requirements:
  - ENTRY-03
user_setup: []

must_haves:
  truths:
    - "用户点击某行的复制按钮后，系统创建一条新记录，包含源行的日期、车间和所有可编辑字段数据"
    - "复制后新行高亮显示，原行数据不受任何影响"
    - "复制按钮在操作区域可见且不影响现有的批量删除功能"
  artifacts:
    - path: "public/js/app.js"
      provides: "handleCopyRow 方法和复制按钮 UI"
      contains: "handleCopyRow"
    - path: "public/css/theme.css"
      provides: "复制按钮样式（如需）"
  key_links:
    - from: "public/js/app.js (copy button)"
      to: "handleCopyRow method"
      via: "@click binding"
      pattern: "handleCopyRow"
    - from: "handleCopyRow"
      to: "API.post + loadData"
      via: "POST /:dept/records with editable field values"
      pattern: "API\\.post.*records"
---

<objective>
实现 ENTRY-03 一键复制行功能 + 用户验证全部三个录入体验需求

Purpose: 复制行是高频操作（同车间连续多天数据相似），减少重复录入；最终人工验证三个需求在浏览器中的真实交互
Output: app.js 中 handleCopyRow 方法、复制按钮 UI、用户验收
</objective>

<execution_context>
@C:/Users/DELL/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DELL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-entry-experience/02-RESEARCH.md
@.planning/phases/02-entry-experience/02-01-SUMMARY.md

@public/js/app.js
@public/css/theme.css
</context>

<interfaces>
<!-- Plan 01 创建的接口（executor 需要知道） -->

Plan 01 新增到 DeptRecordsPage methods:
```javascript
handleTabKey(row, currentField, event)  // Tab 跳转（已实现）
```

已有接口（复用）:
```javascript
editableColumns  // computed: this.columns.filter(c => c.editable)
newRowId         // data: 高亮新增行的 ID
API.post(`/${this.dept}/records`, body)  // 创建记录，body 含 record_date + workshop_id + 字段值
loadData()       // 重新加载表格数据
```

GET /:dept/records 返回的每条记录包含:
```javascript
row.id            // 记录 ID
row.record_date   // 日期
row.workshop_id   // 车间 ID（整数外键，SELECT r.* 包含此字段）
row.workshop_name // 车间名（JOIN 后的显示名）
row.worker_count  // 等等所有字段...
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ENTRY-03 一键复制行功能实现</name>
  <files>public/js/app.js, public/css/theme.css</files>
  <action>
**1. 在 DeptRecordsPage 的 el-table 中添加操作列（app.js 第 357 行 `</el-table>` 之前）：**

在 `v-for="col in columns"` 的 `el-table-column` 之后、`</el-table>` 之前，添加一个固定在右侧的操作列:

```html
<el-table-column label="操作" width="60" fixed="right" align="center">
  <template #default="{ row }">
    <el-tooltip content="复制此行数据为新行" placement="top" :show-after="500">
      <el-button type="primary" link size="small" @click="handleCopyRow(row)"
                 :icon="ElIconDocumentCopy" style="padding:4px;">
        <span style="font-size:14px;">&#128203;</span>
      </el-button>
    </el-tooltip>
  </template>
</el-table-column>
```

注意：CDN 版本的 Element Plus 图标用法可能不同。如果 ElIconDocumentCopy 不可用，改用纯文本按钮:
```html
<el-button type="primary" link size="small" @click="handleCopyRow(row)" title="复制此行">
  复制
</el-button>
```

**2. 在 DeptRecordsPage 的 methods 中新增 handleCopyRow 方法（在 handleBatchDelete 方法之后）：**

```javascript
async handleCopyRow(row) {
  // 收集源行的所有可编辑字段值
  const body = {
    record_date: row.record_date,
    workshop_id: row.workshop_id,
  };
  // 遍历可编辑列，复制数值（跳过 record_date 避免重复）
  for (const col of this.editableColumns) {
    if (col.field !== 'record_date' && body[col.field] === undefined) {
      body[col.field] = row[col.field] ?? 0;
    }
  }

  this.saving = true;
  try {
    const res = await API.post(`/${this.dept}/records`, body);
    ElementPlus.ElMessage.success('复制成功，已生成新行');
    await this.loadData();
    // 高亮新行，复用 newRowId 机制
    const newId = res.data?.id;
    if (newId) {
      this.newRowId = newId;
      setTimeout(() => { this.newRowId = null; }, 4000);
    }
  } catch (err) {
    ElementPlus.ElMessage.error('复制失败: ' + (err.message || '未知错误'));
  } finally {
    this.saving = false;
  }
}
```

**3. 样式（theme.css，如需）：**

如果操作列的"复制"按钮需要样式微调，在 theme.css 中添加:

```css
/* 明细表操作列 - 复制按钮 */
.el-table .el-button--link.el-button--primary {
  color: var(--primary-color, #7F41C0);
}
.el-table .el-button--link.el-button--primary:hover {
  color: var(--primary-hover, #6A35A3);
}
```

**注意事项：**
- `row.workshop_id` 在 GET 返回中确认存在（SELECT r.* 包含此字段），直接使用
- 复制使用源行日期（符合"复制"语义），用户可在新行中单独修改
- `this.saving = true` 防止重复点击
- 复用 `newRowId` + `new-row-highlight` 高亮动画
- 不修改原行数据（只读取 `row[col.field]`）
  </action>
  <verify>
    <automated>cd "D:/03-AI related/02-Business data statistics" && npm test -- --verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
1. el-table 中新增了固定右侧的"操作"列，内含"复制"按钮
2. handleCopyRow 方法完整实现：收集可编辑字段 -> POST 创建 -> 刷新数据 -> 高亮新行
3. 复制按钮使用项目颜色规范（深晶紫 #7F41C0）
4. 所有测试通过
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 用户验证全部三个录入体验需求</name>
  <files>N/A</files>
  <action>
用户在浏览器中验证 Phase 2 全部三个录入体验功能（ENTRY-01/02/03）。
已自动完成的工作:
- ENTRY-01: 快捷新增行（迷你弹窗只填日期+车间，创建后自动聚焦第一个可编辑单元格）
- ENTRY-02: Tab 键行内跳转（按 Tab 跳到下一个可编辑字段，Shift+Tab 反向，跳过计算列）
- ENTRY-03: 一键复制行（点击"复制"按钮，生成包含源行数据的新行）

请在浏览器中打开系统（http://localhost:6001），登录后进入任意部门明细表，按顺序测试:

**测试 1 -- ENTRY-01 快捷新增行:**
1. 点击"+ 新增"按钮
2. 弹窗中只填写日期和车间，点击"确认创建"
3. 预期：弹窗关闭，表格出现新行（高亮闪烁），且第一个可编辑单元格（如"管工人数"）自动进入编辑状态，光标闪烁
4. 新行的数值字段应全为 0

**测试 2 -- ENTRY-02 Tab 键跳转:**
1. 点击任意一行的"管工人数"单元格进入编辑
2. 输入一个数值，按 Tab 键
3. 预期：当前值保存，焦点跳到同一行的下一个可编辑字段（"员工人数"）
4. 继续按 Tab，应跳过"产值/天"后面的所有计算列（结余金额、结余%等）
5. 按 Shift+Tab 应回到前一个可编辑字段
6. 在最后一个可编辑字段按 Tab，应退出编辑模式（不跳行）

**测试 3 -- ENTRY-03 复制行:**
1. 在表格中找到一行有数据的记录
2. 点击该行右侧"操作"列的"复制"按钮
3. 预期：出现"复制成功"提示，表格新增一行（高亮），新行的日期、车间和所有数值与源行相同
4. 检查原行数据是否保持不变
5. 刷新页面，确认复制的行仍然存在
  </action>
  <verify>用户输入 "approved" 表示全部通过，或描述发现的问题</verify>
  <done>用户确认 ENTRY-01/02/03 三项功能全部在浏览器中正常工作</done>
</task>

</tasks>

<verification>
- `npm test` 完整测试套件通过
- 搜索 app.js 确认 `handleCopyRow` 方法存在
- 搜索 app.js 确认操作列 `el-table-column` 存在
- 三项功能均通过人工浏览器验证
</verification>

<success_criteria>
1. ENTRY-03: 复制按钮可见，点击后创建包含源行数据的新行，原行不受影响
2. 用户在浏览器中验证 ENTRY-01/02/03 全部三项功能正常工作
3. 所有自动化测试通过
</success_criteria>

<output>
After completion, create `.planning/phases/02-entry-experience/02-02-SUMMARY.md`
</output>
