---
phase: 01-bug
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - routes/import-export.js
  - public/js/app.js
  - tests/bug-fixes.test.js
autonomous: false
requirements:
  - BUG-11
  - BUG-12

must_haves:
  truths:
    - "Excel 导入成功后，数据在页面中正确显示，刷新后仍然存在"
    - "河源车间 department=NULL 不导致前端功能异常"
    - "Jest 回归测试覆盖 BUG-01 到 BUG-07、BUG-11、BUG-12 的核心场景"
    - "三个部门基础 CRUD（创建、查询、更新、删除）测试通过"
  artifacts:
    - path: "tests/bug-fixes.test.js"
      provides: "BUG 回归测试 + 三部门 CRUD 测试"
      min_lines: 100
    - path: "routes/import-export.js"
      provides: "BUG-12 Excel 导入修复（如需代码修改）"
  key_links:
    - from: "tests/bug-fixes.test.js"
      to: "routes/records.js, routes/workshops.js, routes/settings.js"
      via: "HTTP 集成测试验证路由行为"
      pattern: "describe.*BUG"
    - from: "routes/import-export.js COLUMN_MAP"
      to: "modules/balance/config.js"
      via: "Excel 列名到数据库字段的映射"
      pattern: "COLUMN_MAP"
---

<objective>
修复剩余 2 个需调查的 BUG（BUG-11 河源车间 NULL、BUG-12 Excel 导入不显示），并为全部 12 个 BUG 编写 Jest 回归测试套件。

Purpose: BUG-12 是用户反馈的最直观问题（导入成功但看不到数据），需要运行时调查。BUG-11 需要确认 NULL 值是否导致实际功能异常。回归测试套件确保修复不被未来改动破坏。
Output: BUG-11/12 修复 + tests/bug-fixes.test.js 回归测试文件
</objective>

<execution_context>
@C:/Users/DELL/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DELL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bug/01-RESEARCH.md
@.planning/phases/01-bug/01-VALIDATION.md
@.planning/codebase/TESTING.md

@routes/import-export.js
@routes/records.js
@routes/workshops.js
@routes/settings.js
@public/js/app.js
@modules/balance/config.js
@modules/index.js
@db/postgres.js
@tests/modules.test.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: 调查并修复 BUG-12 Excel 导入不显示 + BUG-11 河源车间 NULL 处理</name>
  <files>routes/import-export.js, public/js/app.js</files>
  <action>
**BUG-12 调查修复流程（按用户决定：重点排查 COLUMN_MAP 映射和 INSERT 语句）：**

1. **Read routes/import-export.js 完整文件。** 重点关注：
   - COLUMN_MAP 定义：每个部门的 Excel 列名 -> 数据库字段名映射
   - 导入处理逻辑：如何读取 Excel 行、映射字段、执行 INSERT
   - 跳过条件：哪些情况下会 `continue` 跳过某行（约第92-94行，workshop_name 为空跳过）
   - console.log 调试日志位置（约第41、46行）

2. **排查最可能的根因（按 Research 分析）：**
   - 手动编辑 Excel 列名是否与 COLUMN_MAP 中的 label/alias 完全匹配（注意空格、全角字符）
   - `cleanColumnName` 函数是否能处理手动编辑引入的特殊字符
   - record_date 解析是否正确（手动编辑的日期格式可能与 SheetJS 解析不同）
   - 如果 COLUMN_MAP 匹配有问题，修复映射逻辑使其更鲁棒（如 trim、忽略多余空格）

3. **修复方案（根据调查结果）：**
   - 如果是 COLUMN_MAP 匹配问题：增强 cleanColumnName 清理逻辑，或在匹配时做 trim/normalize
   - 如果是 workshop_name 映射问题：检查导入时 workshop_name -> workshop_id 的查找逻辑
   - 如果是 INSERT SQL 问题：修复 SQL 语句
   - 确保导入成功后前端自动刷新数据（Research 确认 app.js uploadFile 已调用 loadData()，但需确认调用是否正确触发）

4. **导入成功后交互（用户锁定决定）：** 静默刷新表格——显示简短成功提示 + 自动重新加载数据列表。不做预览（Phase 4 内容）。

**BUG-11 河源车间 NULL 处理：**

5. **在 app.js 中搜索所有使用 department 字段的位置：**
   - 查找下拉过滤、列表渲染、分组等逻辑中对 department 的引用
   - 确认 NULL department 是否导致 JavaScript 报错（如 `.toLowerCase()` on null）或渲染异常
   - 河源车间按设计是"暂无部门分配"，NULL 是正确状态（CLAUDE.md 4.1）

6. **如有前端代码因 NULL 报错：** 添加防御性处理（如 `|| ''` 或跳过 NULL 车间），不要修改数据库数据。
   **如无功能异常：** 在 SUMMARY 中记录"BUG-11 确认 NULL 值被正确处理，无需代码修改"。
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
- BUG-12：Excel 导入成功后数据在页面正确显示，刷新后仍然存在
- BUG-11：河源车间 department=NULL 被正确处理（防御性代码或确认无异常）
- 现有测试全部通过
  </done>
</task>

<task type="auto">
  <name>Task 2: 编写 Jest 回归测试套件（全部 BUG + 三部门 CRUD）</name>
  <files>tests/bug-fixes.test.js</files>
  <action>
创建 `tests/bug-fixes.test.js`，覆盖用户要求的"BUG 回归测试 + 三个部门基础 CRUD"。

**测试基础设施：**
- 直接使用 `db/postgres.js` 连接测试数据库（localhost:5432/production_system）
- 测试数据使用独特标识（如 record_date = '2099-01-01'）方便清理
- afterAll 清理测试数据并关闭数据库连接池

**BUG 回归测试（按可自动化的 BUG，参照 VALIDATION.md）：**

BUG-01（路由顺序）：
- 测试：发送 DELETE /api/beer/records/batch 请求，确认返回非 404（不被 /:id 拦截）

BUG-02（workshop_name 字段）：
- 测试：GET /api/beer/records 返回数据中包含 workshop_name 字段

BUG-03（workshops company 字段）：
- 测试：POST /api/workshops 发送含 company 的数据，GET 返回时 company 字段有值

BUG-04（锁定字段名）：
- 测试：POST /api/settings/data-locks 发送 { department: 'beer', lock_month: '2099-01' }，不返回 500 错误

BUG-05（汇总 SQL）：
- 测试：GET /api/beer/records/summary 返回数据包含独有费用字段（如 machine_repair）

BUG-06（init.sql company 列）：
- 测试：读取 init.sql 文件内容，断言包含 'company' 字符串

BUG-07（locked_by_name）：
- 测试：GET /api/settings/data-locks 返回数据中包含 locked_by_name 字段

BUG-11（河源车间 NULL）：
- 测试：查询 workshops 表中 region='河源' 的记录，验证 department 允许为 NULL（或有防御值）

BUG-12（Excel 导入）：
- 测试：构造一个简单的 Excel buffer（SheetJS），POST /api/print/import，然后 GET /api/print/records 确认数据存在

**三部门基础 CRUD 测试：**
对 beer/print/assembly 各测试：
- POST 创建一条记录（最少必填字段：record_date, workshop_id）
- GET 查询该记录存在
- PUT 更新该记录的某个字段
- DELETE 删除该记录

**测试风格：** 遵循项目现有模式（tests/modules.test.js）：
- describe 嵌套按功能分组
- 中文测试名描述行为
- 使用 expect/toBe/toEqual 等 Jest 内置断言

**HTTP 请求方式：** 使用 supertest 或直接 fetch/axios 调用 localhost:6001（确认 PM2 正在运行）。如果 supertest 未安装，先用 `npm install --save-dev supertest` 安装，然后 require app 对象进行测试。如果 server.js 没有导出 app，可以直接用 axios 对 localhost:6001 发请求（项目已安装 axios 的 CDN 版本，但 Node 端需要额外安装或用 node-fetch）。优先使用 supertest + app 导出方式。

**注意：** 测试需要认证的路由需要先获取 JWT token（POST /api/auth/login 用默认账号 RRxing/admin123）。
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>
- tests/bug-fixes.test.js 文件存在，包含 BUG-01~07、BUG-11、BUG-12 回归测试
- 三部门（beer/print/assembly）基础 CRUD 测试通过
- `npm test` 全部通过（现有测试 + 新增回归测试）
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 用户验证全部 12 个 BUG 修复效果</name>
  <action>
Phase 1 全部 12 个 BUG 修复完成 + 回归测试套件。用户在浏览器中逐项验证修复效果。
  </action>
  <verify>用户在浏览器中确认 7 个可视化检查项全部通过</verify>
  <done>用户输入 approved 确认所有修复正常</done>
  <what-built>
Phase 1 全部 12 个 BUG 修复完成 + 回归测试套件：
- P0: 批量删除路由顺序 (BUG-01)、车间 company 字段 (BUG-03/06)、汇总 SQL (BUG-05)
- P1: 车间列显示 (BUG-02)、锁定字段名 (BUG-04)、锁定人姓名 (BUG-07)
- P2: 动态下拉 (BUG-08/09)、CSS 变量 (BUG-10)、河源车间 (BUG-11)
- 导入修复 (BUG-12)
- 安全修复：单删锁定检查 + modulePermission
- 回归测试：tests/bug-fixes.test.js
  </what-built>
  <how-to-verify>
请在浏览器中打开 http://localhost:6001 逐项检查：

1. **BUG-02 车间列：** 打开任一部门明细表，确认"车间"列显示车间名称（如"兴信A"），不是空白
2. **BUG-04 数据锁定：** 进入系统设置 > 数据锁定，选择部门和月份，点击锁定，确认操作成功（不报错）
3. **BUG-07 锁定人：** 在数据锁定列表中，确认"锁定人"列显示姓名（如"RRxing"），不是数字
4. **BUG-08 锁定部门下拉：** 锁定弹窗的部门下拉框正确显示三个部门
5. **BUG-09 用户管理下拉：** 新增/编辑用户弹窗的部门下拉框正确显示三个部门
6. **BUG-10 编辑框边框：** 在明细表中进入编辑模式，确认输入框边框颜色为紫色（与导航栏主色一致）
7. **BUG-12 Excel 导入：** 上传一个印喷部 Excel 文件，确认导入成功后数据在表格中显示，刷新页面后仍在

如果 ALTER TABLE workshops ADD COLUMN company 尚未执行，请先在数据库中执行：
```sql
ALTER TABLE workshops ADD COLUMN IF NOT EXISTS company VARCHAR(100);
```
  </how-to-verify>
  <resume-signal>输入 "approved" 确认所有修复正常，或描述发现的问题</resume-signal>
</task>

</tasks>

<verification>
- `npm test` 全部通过（现有 24 个 + 新增 bug-fixes.test.js）
- 用户在浏览器中验证 7 个可视化检查项
- 所有 BUG 不再复现
</verification>

<success_criteria>
- 12 个 BUG 全部修复，不存在数据显示错误、操作静默失败或数据保存丢失
- 2 个安全漏洞已修复（单删锁定检查 + modulePermission）
- 回归测试套件覆盖核心场景，npm test 全量通过
- 用户在浏览器中确认所有可视化修复项正常
</success_criteria>

<output>
After completion, create `.planning/phases/01-bug/01-03-SUMMARY.md`
</output>
