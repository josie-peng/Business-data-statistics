---
phase: 01-bug
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/bug-fixes.test.js
autonomous: true
requirements:
  - BUG-01
  - BUG-02
  - BUG-03
  - BUG-04
  - BUG-05
  - BUG-06
  - BUG-07
  - BUG-11
  - BUG-12

must_haves:
  truths:
    - "tests/bug-fixes.test.js 存在且包含每个可自动化 BUG 的失败断言"
    - "npm test -- --testPathPattern=bug-fixes 运行时测试文件被识别并执行"
    - "所有 BUG 相关测试当前为 RED 状态（失败），证明缺陷确实存在"
  artifacts:
    - path: "tests/bug-fixes.test.js"
      provides: "BUG-01~07、BUG-11、BUG-12 回归测试骨架 + 三部门 CRUD 测试骨架"
      min_lines: 80
  key_links:
    - from: "tests/bug-fixes.test.js"
      to: "routes/records.js, routes/workshops.js, routes/settings.js"
      via: "HTTP 集成测试验证路由行为"
      pattern: "describe.*BUG"
---

<objective>
创建 BUG 回归测试骨架文件（Wave 0），使后续 Wave 1 修复任务的 `npm test` 验证具有实际意义。

Purpose: 没有这个测试文件，Plans 01-01 和 01-02 的 `npm test` 只运行现有 24 个无关测试，无法检测 BUG 修复是否正确。Wave 0 先写失败测试，Wave 1 修复代码使测试变绿，符合 TDD RED-GREEN 原则。
Output: tests/bug-fixes.test.js 包含所有可自动化 BUG 的失败断言
</objective>

<execution_context>
@C:/Users/DELL/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/DELL/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bug/01-RESEARCH.md
@.planning/phases/01-bug/01-VALIDATION.md
@.planning/codebase/TESTING.md

@routes/records.js
@routes/workshops.js
@routes/settings.js
@routes/import-export.js
@modules/balance/config.js
@db/postgres.js
@tests/modules.test.js
@server.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 tests/bug-fixes.test.js 测试骨架（RED 状态）</name>
  <files>tests/bug-fixes.test.js</files>
  <action>
创建 `tests/bug-fixes.test.js`，包含以下测试用例。所有测试此时应为 RED（失败），因为对应的 BUG 尚未修复。

**测试基础设施：**
- 检查 supertest 是否已安装（`require.resolve('supertest')`），如未安装则先 `npm install --save-dev supertest`
- 检查 server.js 是否导出 app 对象。如果没有导出，在 server.js 末尾添加 `module.exports = app`（仅添加导出，不修改其他代码）。需要条件导出以避免影响 PM2 启动：`if (process.env.NODE_ENV === 'test') module.exports = app; ` 或直接 `module.exports = app;`
- 测试数据使用独特标识（record_date = '2099-01-01'）方便清理
- afterAll 清理测试数据并关闭数据库连接池
- 需要认证的路由先获取 JWT token（POST /api/auth/login 用 RRxing/admin123）

**BUG 回归测试（describe('BUG 回归测试')）：**

BUG-01（路由顺序）：
- `test('BUG-01: DELETE /api/beer/records/batch 不被 /:id 拦截')`
- 发送 DELETE /api/beer/records/batch，body 为 `{ ids: [] }`
- 断言：返回状态码不是 404（如果路由顺序正确，应返回 200 或 400，而非 404）

BUG-02（workshop_name 字段）：
- `test('BUG-02: GET /api/beer/records 返回 workshop_name 字段')`
- 发送 GET /api/beer/records
- 断言：返回数据中记录对象包含 workshop_name 属性

BUG-03（workshops company 字段）：
- `test('BUG-03: POST /api/workshops 保存 company 字段')`
- 创建一个测试车间（name='测试车间2099', company='测试公司', department='beer'）
- GET 查询该车间，断言 company 字段有值
- afterAll 中清理此测试车间

BUG-04（锁定字段名）：
- `test('BUG-04: POST /api/settings/data-locks 接受 department 字段')`
- 发送 POST /api/settings/data-locks，body 为 `{ department: 'beer', lock_month: '2099-01' }`
- 断言：不返回 500 错误（字段名匹配则能正常处理）
- afterAll 中清理此测试锁定记录

BUG-05（汇总 SQL 覆盖独有字段）：
- `test('BUG-05: GET /api/beer/records/summary 返回独有费用字段')`
- 发送 GET /api/beer/records/summary（带 month 参数）
- 断言：返回数据包含 beer 独有费用字段（如 machine_repair）

BUG-06（init.sql company 列）：
- `test('BUG-06: init.sql 包含 workshops 表 company 列')`
- 读取 db/init.sql 文件内容
- 断言：文件内容包含 'company' 字符串（在 workshops CREATE TABLE 上下文中）

BUG-07（locked_by_name）：
- `test('BUG-07: GET /api/settings/data-locks 返回 locked_by_name 字段')`
- 发送 GET /api/settings/data-locks
- 断言：如果有数据，记录对象包含 locked_by_name 属性

BUG-11（河源车间 NULL department）：
- `test('BUG-11: workshops 中 department=NULL 的记录不导致查询错误')`
- 发送 GET /api/workshops
- 断言：返回 200 且 data 是数组（NULL 值不导致崩溃）

BUG-12（Excel 导入）：
- `test('BUG-12: Excel 导入后数据存在于数据库')`
- 使用 SheetJS（xlsx 库，项目已安装）构造一个最小 Excel buffer
- POST /api/beer/import 上传
- GET /api/beer/records 查询，断言导入的数据存在
- afterAll 中清理导入的测试数据

**三部门基础 CRUD 测试（describe('三部门基础 CRUD')）：**
对 beer/print/assembly 各编写：
- POST 创建一条记录（record_date='2099-01-01', workshop_id=从数据库查询第一个该部门车间的 id）
- GET 查询该记录存在
- PUT 更新该记录某字段
- DELETE 删除该记录

**测试风格：** 遵循 tests/modules.test.js 模式，describe 嵌套，中文测试名。

**关键注意：** 此任务的目标是写出能被 Jest 发现和执行的测试。部分测试此时会失败（RED）是预期行为。但测试文件本身不能有语法错误或导入错误——文件必须能成功加载，只是断言失败。

**验证方式：** 运行 `npm test -- --testPathPattern=bug-fixes` 确认测试文件被发现并执行（允许测试失败，但不允许文件加载失败）。
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=bug-fixes 2>&1 | head -5</automated>
  </verify>
  <done>
- tests/bug-fixes.test.js 文件存在且可被 Jest 加载执行
- 包含 BUG-01~07、BUG-11、BUG-12 的测试用例
- 包含三部门 CRUD 测试骨架
- 部分测试为 RED 状态（预期的失败，因为 BUG 未修复）
- 文件无语法错误、无导入错误
  </done>
</task>

</tasks>

<verification>
- `npm test -- --testPathPattern=bug-fixes` 能发现并运行测试文件（不报 SyntaxError 或 ModuleNotFoundError）
- 测试结果中可见 BUG-01 到 BUG-12 相关的 describe/test 名称
- 现有 24 个测试（modules.test.js）不受影响
</verification>

<success_criteria>
- tests/bug-fixes.test.js 存在且 Jest 能加载执行
- 每个可自动化的 BUG（01-07、11、12）有对应测试用例
- 三部门 CRUD 测试骨架就位
- 后续 Wave 1 计划（01-01、01-02）的修复可以通过运行这些测试验证效果
</success_criteria>

<output>
After completion, create `.planning/phases/01-bug/01-00-SUMMARY.md`
</output>
